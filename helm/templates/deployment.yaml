apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-bpcalc
  labels:
    app: bpcalc
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: bpcalc
  template:
    metadata:
      labels:
        app: bpcalc
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 80
              protocol: TCP